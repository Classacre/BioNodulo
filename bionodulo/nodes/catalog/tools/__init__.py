"""Typed catalog authoring modules for external tools.

The catalog package deliberately contains only declarative specifications and
small execution-plan factories.  Legacy ``CommandNode`` implementations remain
the runtime bridge until the v2 executor is enabled for a family.
"""

